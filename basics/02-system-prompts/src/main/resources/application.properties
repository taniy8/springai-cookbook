# OpenAI API key - set this as an environment variable, never hardcode it
spring.ai.openai.api-key=${OPENAI_API_KEY}

# Model to use
spring.ai.openai.chat.options.model=gpt-4o-mini

# Server port
server.port=8080
